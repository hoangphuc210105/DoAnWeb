@model IEnumerable<DoAnWeb.Models.Giohang>

@{
    ViewData["Title"] = "Giỏ hàng của bạn";
    Layout = "_Layout";
}

<h2 class="text-center fw-bold mb-4" style="color:#333;">@ViewData["Title"]</h2>

@if (!Model.Any())
{
    <div class="alert alert-info">
        Giỏ hàng của bạn đang trống.
        <a asp-controller="Phone" asp-action="Index" class="alert-link">Tiếp tục mua sắm</a>.
    </div>
}
else
{
    // bản code cũ
    @* <table class="table table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>Hình ảnh</th>
                <th>Sản phẩm</th>
                <th>Giá</th>
                <th>Số lượng</th>
                <th>Tổng</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="width:120px">
                        <img src="~/images/@item.MaspNavigation.Hinhanh" alt="@item.MaspNavigation.Tensp" class="img-thumbnail" style="width:100px" />
                    </td>
                    <td>@item.MaspNavigation.Tensp</td>
                    <td>@String.Format("{0:N0} đ", item.MaspNavigation.Gia)</td>
                    <td>
                        <form asp-action="UpdateQuantity" method="post" class="d-flex">
                            <input type="hidden" name="id" value="@item.Masp" />
                            <input type="number" name="quantity" value="@item.Soluong" min="1" class="form-control form-control-sm me-2" style="width:80px" />
                            <button type="submit" class="btn btn-sm btn-primary">Cập nhật</button>
                        </form>
                    </td>
                    <td>@String.Format("{0:N0} đ", (item.MaspNavigation.Gia ?? 0) * item.Soluong)</td>
                    <td>
                        <a asp-action="Remove" asp-route-id="@item.Masp" class="btn btn-sm btn-danger" onclick="return confirm('Xóa sản phẩm này?')">
                            Xóa
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="text-end">
        <h4>
            Tổng cộng:
            <span class="text-danger">
                @String.Format("{0:N0} đ", Model.Sum(i => (i.MaspNavigation.Gia ?? 0) * i.Soluong))
            </span>
        </h4>

        <a asp-action="Checkout" class="btn btn-success btn-lg mt-3">Tiến hành thanh toán</a>
    </div> *@

    <div class="container my-4 px-4">
    <!-- Bảng giỏ hàng -->
        <div class="row">
            <div class="col-md-7">
                <form asp-action="UpdateCart" method="post">
                    <table class="table align-middle">
                            <thead>
                            <tr class="border-bottom">
                                <th>Sản phẩm</th>
                                <th>Giá</th>
                                <th>Số lượng</th>
                                <th>Tổng</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="border-bottom">
                                    <!-- Hình ảnh + tên -->
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <img src="~/images/@item.MaspNavigation.Hinhanh"
                                                 alt="@item.MaspNavigation.Tensp"
                                                 class="img-thumbnail me-2"
                                                 style="width:60px; height:60px; object-fit:cover;" />
                                            <span>@item.MaspNavigation.Tensp</span>
                                        </div>
                                    </td>
                                    <!-- Giá cả -->
                                    <td>@String.Format("{0:N0} đ", item.MaspNavigation.Gia)</td>
                                    <td>
                                        <input type="hidden" name="id" value="@item.Masp" />
                                        <input type="number" name="quantity" value="@item.Soluong" min="1" class="form-control form-control-sm me-2" style="width:80px" />
                                    </td>
                                    <!-- Tổng cộng -->
                                    <td class="fw-bold text-danger">
                                        @String.Format("{0:N0} đ", (item.MaspNavigation.Gia ?? 0) * item.Soluong)
                                    </td>
                                    <!-- Nút xóa -->
                                    <td>
                                        <a asp-action="Remove" asp-route-id="@item.Masp"
                                           class="btn btn-sm btn-danger"
                                           onclick="return confirm('Xóa sản phẩm này?')">Xóa</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <div class="d-flex justify-content-between mt-3">
                        <a href="@Url.Action("Index", "Phone")" class="btn btn-outline-secondary">← Tiếp tục xem sản phẩm</a>
                        <button type="submit" class="btn btn-primary">Cập nhật giỏ hàng</button>
                    </div>
                </form>
            </div>
            <!-- Tổng tiền -->
            <div class="col-md-5">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">Tổng số lượng</h5>
                        <p class="d-flex justify-content-between">
                            <span>Tổng cộng</span>
                            <span class="fw-bold">@String.Format("{0:N0} đ", Model.Sum(i => (i.MaspNavigation.Gia ?? 0) * i.Soluong))</span>
                        </p>
                        <p class="d-flex justify-content-between">
                            <span>Giao hàng</span>
                            <span class="text-success">Miễn phí</span>
                        </p>
                        <hr />
                        <h4 class="text-end text-danger fw-bold mb-3">
                            @String.Format("{0:N0} đ", Model.Sum(i => (i.MaspNavigation.Gia ?? 0) * i.Soluong))
                        </h4>

                        <a asp-action="Checkout" class="btn btn-danger w-100 mb-3">Tiến hành thanh toán</a>

                    </div>
                </div>
            </div>
        </div>
    </div>
}
